<html lang="en">
  
<%- include("./partials2/head.ejs") %>

<body>
  <%- //include("./partials1/newheader.ejs") %>  
  <%- include("./partials2/nav.ejs") %>

  <div class="blogs content">
    <h2>All Posts</h2>

    <% if (posts.length > 0) { %>
      <% posts.forEach(post => { %>
        <a class="single" href="/posts/<%= post._id %>">
          <h3 class="title"><%= post.startPoint %></h3>
          <p class="snippet"><%= post.endPoint %></p>
        </a>
      <% }) %>
    <% } else { %>
      <p>There are no posts to display...</p>
    <% } %>
    
  </div>

  <%- include("./partials2/footer.ejs") %>
</body>
</html>